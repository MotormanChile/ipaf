# Video "¿Por qué elegir la Formación IPAF?"

Este directorio debe contener el archivo referenciado en `index.html` (sección `#why`):

```
video/formacion-ipaf-plataforma-elevadora.mp4
```

Ahora mismo ese archivo es un placeholder vacío (0 bytes) — sólo para que la
ruta exista en el proyecto. Reemplázalo arrastrando el video final exportado
desde NotebookLM (u otra fuente) **con exactamente ese mismo nombre**, para
no tener que tocar el HTML.

Recomendaciones de formato:
- Contenedor: MP4 (H.264 + AAC)
- Orientación: 16:9 horizontal (el panel donde se muestra es más ancho que alto)
- Duración sugerida: 60-90 segundos
- Si el export de NotebookLM no es MP4 16:9, conviértelo antes (HandBrake, Canva, etc.)
